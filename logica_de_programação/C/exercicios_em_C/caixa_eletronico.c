#include <stdio.h>

int main() 
{
    char nsaque;
    int valor;
    int cedula, total, totced, c = 0;
    do
    {
        printf("Qual valor deseja sacar? R$");
        scanf("%i", &valor);
        total = valor; 
        cedula = 50;
        totced = 0;
        while (total != 0)
        {
            /* code */
        }
        
        printf("%i", cedula);
    } while (nsaque != 'N' || nsaque != 'n');
    
}